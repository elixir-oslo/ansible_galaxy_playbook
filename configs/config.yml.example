# =====================================================================
# Local deployment configuration (EXAMPLE)
#
# Copy this file to:
#   config.yml
#
# ⚠️  DO NOT COMMIT config.yml
# =====================================================================

# ---------------------------------------------------------------------
# Galaxy target host connection details
# ---------------------------------------------------------------------
galaxy:
  host_ip: "use_host_ip"          # Galaxy server IP or DNS name
  ssh_user: "ubuntu"               # SSH user with sudo privileges

# ---------------------------------------------------------------------
# Galaxy administrator settings
# ---------------------------------------------------------------------
admin:
  email: "admin@example.com"       # Galaxy admin email address

# ---------------------------------------------------------------------
# Database configuration (PostgreSQL)
# ---------------------------------------------------------------------
database:
      user: galaxy
      password: "CHANGE_ME_STRONG_PASSWORD"
      host: localhost
      port: 5432
      name: galaxy

# ---------------------------------------------------------------------
# Optional: filesystem layout (defaults used if omitted)
# ---------------------------------------------------------------------
paths:
  galaxy_root: "/srv/galaxy"

# ---------------------------------------------------------------------
# Optional: feature flags
# ---------------------------------------------------------------------
features:
  enable_https: false
  run_bootstrap: true


